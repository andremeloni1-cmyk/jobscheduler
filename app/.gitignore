# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env & secrets
.env
.env.local

# database (SQLite) — never commit data
*.db
*.db-journal
prisma/*.db
prisma/*.db-journal

# misc
*.log
.DS_Store

# Locally-downloaded Prisma engines (re-fetched on install)
/engines/

# TypeScript incremental build cache
*.tsbuildinfo
